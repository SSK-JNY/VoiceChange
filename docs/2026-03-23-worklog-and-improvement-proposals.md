# 2026-03-23 作業ログと改善提案

## 1. 目的

本ドキュメントは、当日の作業内容を「チャット作業ログ」と「Git 差分（現時点）」の両方から整理した記録です。

## 2. Git 差分サマリ（現時点）

- 変更ファイル数: 1
- 変更ファイル:
  - README.md
- 差分統計:
  - 81 insertions(+), 66 deletions(-)

### 2.1 現在の未コミット変更

- README.md
  - 依存関係・既定値・モデル配置説明・オーディオモード説明など、実装整合性の修正

## 3. ログベースの当日実施内容（時系列要約）

以下は、当日の作業ログから抽出した実施内容です（すでに反映済み/コミット済みの可能性あり）。

1. GUI 操作性の改善

- ウィンドウのリサイズ対応
- スクロール（縦/横）対応
- ボタン表示・操作導線の調整

2. UI 機能整理

- 非対応のモデルダウンロード導線を整理
- パススルーモード（エフェクト無し）の導入

3. 音声処理チューニング

- ノイズゲート強化（無音帯の抑制改善）
- フォルマント効果を体感しやすいよう補正帯域を調整
- 日本語母音（特に お/え）混同を減らすための帯域調整

4. RVC 処理順の見直し

- RVC 前に過度な加工を入れない構成へ変更
- 母音誤認を減らすため、RVC 入力をより素の音声寄りに調整

5. 語尾つぶれ対策

- ノイズゲートの閾値/リリース/ホールド等を語尾保護寄りに調整

6. index 有効時の区間ループ対策

- 失敗時の再利用音声（キャッシュ/ホールド）の使い回しを抑える方向で対策

7. 初期ロボ化パラメータの見直し

- Distortion / Chorus の初期値を 0 に設定（初期はロボ化オフ）

8. README 整合性修正

- 実装と食い違う記載（例: blocksize 既定値、numpy 記載、モデル配置説明）を更新

## 4. README 修正ポイント（今回の主対象）

- 実装との不整合を是正
  - numpy 記載の現状化
  - blocksize 既定値の明確化（コード既定値と設定ファイル優先ルール）
  - モデル配置説明の一貫化
  - ロボ化初期値（0.0 / 0.0）を追記
  - オーディオモード（normal/passthrough/test-tone）を追記
  - 最終更新日の更新

## 5. 追記: 新機能提案・今後の修正提案

### 5.1 優先度 高

1. `index_rate` 有効時の安全ガード

- `index_rate` が高い時は自動でタイムアウト/キャッシュ寿命を安全側に寄せる
- 目的: 区間ループ・断片反復の再発防止

2. 失敗時フォールバック戦略の明示的状態管理

- 「RPC失敗連続回数」「hold再利用回数」を UI/ログで可視化
- 目的: 音切れ/ループの原因を運用時に即判別可能にする

### 5.2 優先度 中

3. 音声品質プリセットの追加

- 低遅延 / バランス / 高品質（完全変換）をワンクリック化
- index 使用時は推奨組み合わせをプリセットで提供

4. 母音保持モード（日本語向け）

- RVC 有効時の後段フォルマント強度を自動抑制
- 目的: お→え などの母音崩れ軽減

5. 語尾保護の ON/OFF 切替

- ノイズゲートの語尾保護設定をトグル化
- 目的: ノイズ抑制優先と明瞭度優先を使い分け可能にする

### 5.3 優先度 低

6. README と scripts/README の重複整理

- 導線を一本化し、詳細は scripts/README に集約
- 目的: 保守性向上、記載更新漏れの防止

7. 既定値の単一ソース化

- README へ値を直書きせず、設定クラスから自動生成する仕組みを検討
- 目的: ドキュメントと実装の乖離防止

## 6. 補足

- 本文中の「ログベース実施内容」は当日の作業記録からの要約です。
- 最終的な反映状態は `git status` / `git diff` / 既存コミット履歴で確認してください。
